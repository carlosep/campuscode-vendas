<h1 class="ls-title-intro ls-ico-cart"><%= t('.orders') %></h1>

<% @orders.each do |order| %>
  <%= link_to order_path(order) do %>
    <strong><%= order.id %></strong> |
    <%= order.product.name %>|
    <%= order.status %></p><br>
  <% end %>
<% end %>
